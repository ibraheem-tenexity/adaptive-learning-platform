# Adaptive Learning Platform

AI-powered adaptive learning with mastery-loop mechanics.

## Setup

1. Set `DATABASE_URL` environment variable
2. Run `npm install`
3. Run `npm run db:migrate`
4. Run `npm run dev`
